Write a Ticket booking system 
There are only two seats available
Display the following menu repetedly:
  1.New boioking
  2.Cancel booking
  3.View booking
  0.Quit

  1.New Booking
    -Enter the customer's name, age, and seat number.
    -Seat number must be between 1 and 2.
    -If the seat number is invalid, print "Not valid".
    -If valid, print "Successfully booked".
    -Display the number of available seats.
2.Cancel Booking
    -Enter the customer's name and seat number.
    -Check whether the seat number is valid.
    -If valid, cancel the booking and display the available seats. 
3.View Booking
    -If there are no bookings, print "Booked seats are not available".
    -Otherwise, display the name, age, and seat number.
0.Quit
    -If the user selects 0, print "Quit" and terminate the program.


    If the user enters any other option:
     print:Invalid option